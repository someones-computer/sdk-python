# coding: utf-8

"""
    someones.computer API

    JSON-LD resources backing the control panel. Bearer-token reachable since #1418 — see docs/sdk-generation.md.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from someones_computer_sdk.models.proxmox_instance import ProxmoxInstance
from typing import Optional, Set
from typing_extensions import Self

class Machine(BaseModel):
    """
    Machine
    """ # noqa: E501
    instance: Optional[ProxmoxInstance] = None
    node: Optional[StrictStr] = Field(default=None, description="The node within the instance — same string `app:proxmox:template` builds on.")
    vmid: Optional[StrictInt] = None
    name: Optional[StrictStr] = Field(default=None, description="How an operator addresses it; also the guest hostname the seed sets.")
    size: Optional[StrictStr] = None
    disk_blocks: Optional[StrictInt] = Field(default=1, description="Disk as a block count, never gigabytes: a thin volume grows and never shrinks, so the invariant worth enforcing is \"this number only goes up\", and a count makes that checkable where a free-form size would not.", alias="diskBlocks")
    template_vmid: Optional[StrictInt] = Field(default=None, description="The template this was cloned from, by vmid. The reference count that decides when a superseded template may be deleted (§03): linked clones pin their base disk, and Proxmox will refuse — rightly — to delete a template that still has children.", alias="templateVmid")
    address: Optional[StrictStr] = Field(default=None, description="The island address chosen by the control plane's pool and registered with Proxmox, which is what dnsmasq then answers the machine's DHCP request with (docs/island-network.md — Proxmox's IPAM cannot allocate, so the platform still picks). Null until allocation; unique so the pool cannot double-allocate even if two provisions race.")
    state: Optional[StrictStr] = 'requested'
    failure: Optional[StrictStr] = Field(default=None, description="Why `Failed`, when it is. Carries the task's own words, never a paraphrase.")
    provision_attempt: Optional[StrictInt] = Field(default=1, description="Which run through the flow this is, starting at 1 and incremented on every {@see self::reprovision()}. {@see \\App\\Entity\\ProvisioningLogLine} tags each captured line with the value that was current when it was written, so a retry's transcript starts fresh rather than appending to the failed attempt before it.", alias="provisionAttempt")
    provision_started_at: Optional[datetime] = Field(default=None, description="When the current run through the flow began — set at construction and reset on every {@see self::reprovision()}, mirroring {@see ProxmoxInstance::$templateBuildStartedAt}. What {@see self::isProvisioningStale()} measures against: the real ceiling is the flow's own step deadlines ({@see \\App\\MessageHandler\\ProvisionMachineHandler}'s `BOOT_DEADLINE`/`TASK_DEADLINE`), and a run still going past {@see self::PROVISION_PRESUMED_DEAD_AFTER} is a worker that died holding it — a crash, an OOM, a deploy restart — rather than one still working.", alias="provisionStartedAt")
    swarm: Optional[StrictStr] = Field(default=None, description="The context this machine serves, if any. Nullable on purpose — see the class docblock.")
    organization: Optional[StrictStr] = Field(default=None, description="The organization this machine was self-service-provisioned for, or null for one an operator made through `/admin/machines`. Nullable for the same reason `$swarm` is: an admin-made machine belongs to nobody's tenancy, and this column must not invent an owner for it. Set once, at creation, to the same organization that owns the paired `$swarm` — {@see Organization::markDeleted()} detaches it rather than deleting the row, matching how a BYO context's owner is handled.")
    id: Optional[StrictStr] = None
    created_at: Optional[datetime] = Field(default=None, alias="createdAt")
    updated_at: Optional[datetime] = Field(default=None, alias="updatedAt")
    disk_gigabytes: Optional[StrictInt] = Field(default=None, alias="diskGigabytes")
    provisioning_stale: Optional[StrictBool] = Field(default=None, description="Still mid-flow, and has been for longer than any healthy run takes — what {@see \\App\\MessageHandler\\SweepStaleMachineProvisionsHandler} sweeps for. A worker that crashed, OOM'd, or was recycled mid-step leaves the row exactly where it stood; nothing else ever revisits it, since the handler's own guard is \"state is Requested\" and every other step only ever moves the flow forward.", alias="provisioningStale")
    __properties: ClassVar[List[str]] = ["instance", "node", "vmid", "name", "size", "diskBlocks", "templateVmid", "address", "state", "failure", "provisionAttempt", "provisionStartedAt", "swarm", "organization", "id", "createdAt", "updatedAt", "diskGigabytes", "provisioningStale"]

    @field_validator('size')
    def size_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['s', 'm', 'l']):
            raise ValueError("must be one of enum values ('s', 'm', 'l')")
        return value

    @field_validator('state')
    def state_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['requested', 'cloning', 'configuring', 'starting', 'booting', 'running', 'serving', 'retiring', 'failed']):
            raise ValueError("must be one of enum values ('requested', 'cloning', 'configuring', 'starting', 'booting', 'running', 'serving', 'retiring', 'failed')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Machine from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "disk_blocks",
            "failure",
            "provision_attempt",
            "provision_started_at",
            "id",
            "created_at",
            "updated_at",
            "disk_gigabytes",
            "provisioning_stale",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of instance
        if self.instance:
            _dict['instance'] = self.instance.to_dict()
        # set to None if address (nullable) is None
        # and model_fields_set contains the field
        if self.address is None and "address" in self.model_fields_set:
            _dict['address'] = None

        # set to None if failure (nullable) is None
        # and model_fields_set contains the field
        if self.failure is None and "failure" in self.model_fields_set:
            _dict['failure'] = None

        # set to None if provision_started_at (nullable) is None
        # and model_fields_set contains the field
        if self.provision_started_at is None and "provision_started_at" in self.model_fields_set:
            _dict['provisionStartedAt'] = None

        # set to None if swarm (nullable) is None
        # and model_fields_set contains the field
        if self.swarm is None and "swarm" in self.model_fields_set:
            _dict['swarm'] = None

        # set to None if organization (nullable) is None
        # and model_fields_set contains the field
        if self.organization is None and "organization" in self.model_fields_set:
            _dict['organization'] = None

        # set to None if updated_at (nullable) is None
        # and model_fields_set contains the field
        if self.updated_at is None and "updated_at" in self.model_fields_set:
            _dict['updatedAt'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Machine from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "instance": ProxmoxInstance.from_dict(obj["instance"]) if obj.get("instance") is not None else None,
            "node": obj.get("node"),
            "vmid": obj.get("vmid"),
            "name": obj.get("name"),
            "size": obj.get("size"),
            "diskBlocks": obj.get("diskBlocks") if obj.get("diskBlocks") is not None else 1,
            "templateVmid": obj.get("templateVmid"),
            "address": obj.get("address"),
            "state": obj.get("state") if obj.get("state") is not None else 'requested',
            "failure": obj.get("failure"),
            "provisionAttempt": obj.get("provisionAttempt") if obj.get("provisionAttempt") is not None else 1,
            "provisionStartedAt": obj.get("provisionStartedAt"),
            "swarm": obj.get("swarm"),
            "organization": obj.get("organization"),
            "id": obj.get("id"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt"),
            "diskGigabytes": obj.get("diskGigabytes"),
            "provisioningStale": obj.get("provisioningStale")
        })
        return _obj


