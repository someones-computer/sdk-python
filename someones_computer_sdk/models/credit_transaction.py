# coding: utf-8

"""
    someones.computer API

    JSON-LD resources backing the control panel. Bearer-token reachable since #1418 — see docs/sdk-generation.md.

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from someones_computer_sdk.models.credit_transaction_engine_millis import CreditTransactionEngineMillis
from someones_computer_sdk.models.credit_transaction_usage_bytes import CreditTransactionUsageBytes
from someones_computer_sdk.models.user import User
from typing import Optional, Set
from typing_extensions import Self

class CreditTransaction(BaseModel):
    """
    An IMMUTABLE, append-only ledger row for an Organization's shared credit balance. A balance is never mutated in place — it's always `SUM(amountCents)` over `Succeeded` rows (see {@see CreditTransactionRepository::balanceForOrganization()}). Read-only over the API: money movement stays server-driven.  Three kinds of row, one table ({@see CreditTransactionType}):    - a **top-up** is created `Pending` before redirecting to Stripe Checkout,     keyed on `stripeCheckoutSessionId`, then flipped to `Succeeded`/`Failed`     by {@see \\App\\Controller\\StripeWebhookController};   - a **grant** and a **debit** are terminal the moment they are written, so     both are written `Succeeded` — the `Pending` default is a Stripe-webhook     state and is wrong for them — and neither has a Stripe session, which is     why `stripeCheckoutSessionId` is nullable.  A debit additionally carries the hour it bills for and the meter reading it came from, so the ledger can be checked against the usage events rather than taken on trust. One table rather than two because the balance has to be the sum of money in and money out, and a second table would make every balance read a join.
    """ # noqa: E501
    organization: Optional[StrictStr] = None
    type: Optional[StrictStr] = None
    status: Optional[StrictStr] = 'pending'
    amount_cents: Optional[StrictInt] = Field(default=None, description="Signed integer amount in the smallest currency unit (cents for USD).", alias="amountCents")
    currency: Optional[StrictStr] = 'usd'
    stripe_checkout_session_id: Optional[StrictStr] = Field(default=None, description="Stripe Checkout Session id; a top-up row is created Pending against this before redirecting. Null on a grant and on a debit, neither of which has a Stripe session — the *type* is what says which kind of row this is, not whether this column is set.", alias="stripeCheckoutSessionId")
    usage_hour: Optional[datetime] = Field(default=None, description="The clock hour a debit bills for, truncated to the hour and stored UTC.", alias="usageHour")
    resource_kind: Optional[StrictStr] = Field(default=None, description="Which meter wrote this row; null on every non-debit row. The other half of the unique key above, and always set on a debit — never left null the way a top-up or grant's `usageHour` is, or two debits from different meters in the same hour would stop colliding with each other but a debit from the *same* meter twice would also stop colliding with itself.", alias="resourceKind")
    usage_seconds: Optional[StrictInt] = Field(default=None, description="Resolved container-seconds this debit was computed from; null on anything but a compute debit.", alias="usageSeconds")
    unresolved_containers: Optional[StrictInt] = Field(default=None, description="Containers the meter saw start and never saw stop over the billed hour.", alias="unresolvedContainers")
    usage_bytes: Optional[CreditTransactionUsageBytes] = Field(default=None, alias="usageBytes")
    engine_millis: Optional[CreditTransactionEngineMillis] = Field(default=None, alias="engineMillis")
    stripe_event_id: Optional[StrictStr] = Field(default=None, description="Stripe Event id that last transitioned this row; secondary idempotency guard for webhook delivery.", alias="stripeEventId")
    created_by: Optional[User] = Field(default=None, alias="createdBy")
    id: Optional[StrictStr] = None
    created_at: Optional[datetime] = Field(default=None, alias="createdAt")
    updated_at: Optional[datetime] = Field(default=None, alias="updatedAt")
    __properties: ClassVar[List[str]] = ["organization", "type", "status", "amountCents", "currency", "stripeCheckoutSessionId", "usageHour", "resourceKind", "usageSeconds", "unresolvedContainers", "usageBytes", "engineMillis", "stripeEventId", "createdBy", "id", "createdAt", "updatedAt"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['top_up', 'grant', 'debit']):
            raise ValueError("must be one of enum values ('top_up', 'grant', 'debit')")
        return value

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'succeeded', 'failed']):
            raise ValueError("must be one of enum values ('pending', 'succeeded', 'failed')")
        return value

    @field_validator('resource_kind')
    def resource_kind_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['compute', 'storage', 'engine_load', 'registry_storage']):
            raise ValueError("must be one of enum values ('compute', 'storage', 'engine_load', 'registry_storage')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreditTransaction from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "usage_hour",
            "resource_kind",
            "usage_seconds",
            "unresolved_containers",
            "id",
            "created_at",
            "updated_at",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of usage_bytes
        if self.usage_bytes:
            _dict['usageBytes'] = self.usage_bytes.to_dict()
        # override the default output from pydantic by calling `to_dict()` of engine_millis
        if self.engine_millis:
            _dict['engineMillis'] = self.engine_millis.to_dict()
        # override the default output from pydantic by calling `to_dict()` of created_by
        if self.created_by:
            _dict['createdBy'] = self.created_by.to_dict()
        # set to None if stripe_checkout_session_id (nullable) is None
        # and model_fields_set contains the field
        if self.stripe_checkout_session_id is None and "stripe_checkout_session_id" in self.model_fields_set:
            _dict['stripeCheckoutSessionId'] = None

        # set to None if usage_hour (nullable) is None
        # and model_fields_set contains the field
        if self.usage_hour is None and "usage_hour" in self.model_fields_set:
            _dict['usageHour'] = None

        # set to None if resource_kind (nullable) is None
        # and model_fields_set contains the field
        if self.resource_kind is None and "resource_kind" in self.model_fields_set:
            _dict['resourceKind'] = None

        # set to None if usage_seconds (nullable) is None
        # and model_fields_set contains the field
        if self.usage_seconds is None and "usage_seconds" in self.model_fields_set:
            _dict['usageSeconds'] = None

        # set to None if unresolved_containers (nullable) is None
        # and model_fields_set contains the field
        if self.unresolved_containers is None and "unresolved_containers" in self.model_fields_set:
            _dict['unresolvedContainers'] = None

        # set to None if usage_bytes (nullable) is None
        # and model_fields_set contains the field
        if self.usage_bytes is None and "usage_bytes" in self.model_fields_set:
            _dict['usageBytes'] = None

        # set to None if engine_millis (nullable) is None
        # and model_fields_set contains the field
        if self.engine_millis is None and "engine_millis" in self.model_fields_set:
            _dict['engineMillis'] = None

        # set to None if stripe_event_id (nullable) is None
        # and model_fields_set contains the field
        if self.stripe_event_id is None and "stripe_event_id" in self.model_fields_set:
            _dict['stripeEventId'] = None

        # set to None if created_by (nullable) is None
        # and model_fields_set contains the field
        if self.created_by is None and "created_by" in self.model_fields_set:
            _dict['createdBy'] = None

        # set to None if updated_at (nullable) is None
        # and model_fields_set contains the field
        if self.updated_at is None and "updated_at" in self.model_fields_set:
            _dict['updatedAt'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreditTransaction from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "organization": obj.get("organization"),
            "type": obj.get("type"),
            "status": obj.get("status") if obj.get("status") is not None else 'pending',
            "amountCents": obj.get("amountCents"),
            "currency": obj.get("currency") if obj.get("currency") is not None else 'usd',
            "stripeCheckoutSessionId": obj.get("stripeCheckoutSessionId"),
            "usageHour": obj.get("usageHour"),
            "resourceKind": obj.get("resourceKind"),
            "usageSeconds": obj.get("usageSeconds"),
            "unresolvedContainers": obj.get("unresolvedContainers"),
            "usageBytes": CreditTransactionUsageBytes.from_dict(obj["usageBytes"]) if obj.get("usageBytes") is not None else None,
            "engineMillis": CreditTransactionEngineMillis.from_dict(obj["engineMillis"]) if obj.get("engineMillis") is not None else None,
            "stripeEventId": obj.get("stripeEventId"),
            "createdBy": User.from_dict(obj["createdBy"]) if obj.get("createdBy") is not None else None,
            "id": obj.get("id"),
            "createdAt": obj.get("createdAt"),
            "updatedAt": obj.get("updatedAt")
        })
        return _obj


