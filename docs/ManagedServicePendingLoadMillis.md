# ManagedServicePendingLoadMillis

Engine-milliseconds accrued since the last hourly bill, in milliseconds.

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from someones_computer_sdk.models.managed_service_pending_load_millis import ManagedServicePendingLoadMillis

# TODO update the JSON string below
json = "{}"
# create an instance of ManagedServicePendingLoadMillis from a JSON string
managed_service_pending_load_millis_instance = ManagedServicePendingLoadMillis.from_json(json)
# print the JSON string representation of the object
print(ManagedServicePendingLoadMillis.to_json())

# convert the object into a dict
managed_service_pending_load_millis_dict = managed_service_pending_load_millis_instance.to_dict()
# create an instance of ManagedServicePendingLoadMillis from a dict
managed_service_pending_load_millis_from_dict = ManagedServicePendingLoadMillis.from_dict(managed_service_pending_load_millis_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


